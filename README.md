# atcm-functional-prototype-frontend

To run:

npm install - installs node and bower dependencies.
npm start - starts the application on http://localhost:8000/atcm.html#/home
npm test - runs karma unit tests
